# Copy to ~/.config/nag/deploy.env on the VPS and adjust. Loaded by
# deploy.sh.

# Default branch when deploy.sh is invoked without an argument. The
# GitHub Actions workflows pass an explicit branch, overriding this.
NAG_BRANCH=main

# User-scoped systemd unit restarted after a successful pull.
NAG_SERVICE=nag-expo.service
